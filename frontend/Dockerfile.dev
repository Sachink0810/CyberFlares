# Vite dev-server image (HMR-friendly, code bind-mounted from host).
FROM node:22-alpine

WORKDIR /app

# Install deps in a separate layer (cache-friendly)
COPY package.json ./
RUN npm install --silent

EXPOSE 5173

CMD ["npm", "run", "dev"]
